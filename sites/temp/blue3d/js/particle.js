/**
 * Particleground demo
 * @author Jonathan Nicol - @mrjnicol
 */

$(document).ready(function() {
  $('body').particleground({
    dotColor: '#4E90ED',
    lineColor: '#fff',
    lineWidth: '0.1111',
    particleRadius: '9',
    parallaxMultiplier: '5'
  });
});