/**
 * Particleground demo
 * @author Jonathan Nicol - @mrjnicol
 */

$(document).ready(function() {
  $('body').particleground({
    dotColor: '#9a5319',
    lineColor: '#223',
    lineWidth: '0.0001',
    particleRadius: '9',
    parallaxMultiplier: '5'
  });
});