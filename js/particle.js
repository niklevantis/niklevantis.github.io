/**
 * Particleground demo
 * @author Jonathan Nicol - @mrjnicol
 */

$(document).ready(function() {
  $('.side-left').particleground({
    dotColor: '#000000',
    lineColor: '#737373',
    lineWidth: '0.2111',
    particleRadius: '.1',
    parallaxMultiplier: '5'
  });
});
