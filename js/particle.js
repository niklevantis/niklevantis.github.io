/**
 * Particleground demo
 * @author Jonathan Nicol - @mrjnicol
 */

$(document).ready(function() {
  $('body').particleground({
    dotColor: '#fff',
    lineColor: '#fff',
    lineWidth: '0.0001',
    particleRadius: '1',
    parallaxMultiplier: '1'
  });
});