/**
 * Particleground demo
 * @author Jonathan Nicol - @mrjnicol
 */

$(document).ready(function() {
  $('body').particleground({
    dotColor: '#000',
    lineColor: '#292929',
    lineWidth: '1.0001',
    particleRadius: '5',
    parallaxMultiplier: '5'
  });
});